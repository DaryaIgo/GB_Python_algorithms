# Урок 3. Массивы. Кортежи. Множества. Списки

# 7. В одномерном массиве целых чисел определить два наименьших элемента. Они могут быть
# как равны между собой (оба являться минимальными), так и различаться.

from random import random

N = 10
array_1 = [0] * N
for i in range(N):
    array_1[i] = int(random()*100)
print(array_1)

sort_array = sorted(array_1)
print(f'Два наименьших эелемента из массива {sort_array[:2]}')
